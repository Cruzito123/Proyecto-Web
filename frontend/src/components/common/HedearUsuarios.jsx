import React from 'react';

// El CSS se importa en el archivo principal (Mesero.jsx o App.js)
// Así que no necesitamos importar CSS aquí.

// Asegúrate que el nombre de la función sea el mismo que exportas
function HedearUsuarios() { 
  return (
    // Esto es solo el header, no el 'container'
    <header className="dashboard-header">
      <h1>Panel de Mesero - sdadasd</h1>
      <p>Gestión de mesas y pedidos</p>
    </header>
  );
}

export default HedearUsuarios;